local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

local LP = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "TSUNAMI_PREMIER"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.Parent = LP:WaitForChild("PlayerGui")

local Main = Instance.new("Frame")
Main.Size = UDim2.fromScale(0.92,0.80)
Main.Position = UDim2.fromScale(0.04,0.10)
Main.BackgroundColor3 = Color3.fromRGB(0,35,70)
Main.BorderSizePixel = 0
Main.Parent = gui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0,18)
mainCorner.Parent = Main

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(0,200,255)
mainStroke.Thickness = 3
mainStroke.Parent = Main

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,-80,0,70)
Title.Position = UDim2.fromOffset(20,0)
Title.BackgroundTransparency = 1
Title.Text = "🌊 TSUNAMI PREMIER"
Title.TextColor3 = Color3.fromRGB(235,250,255)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

local Close = Instance.new("TextButton")
Close.Size = UDim2.fromOffset(58,58)
Close.Position = UDim2.new(1,-68,0,6)
Close.BackgroundColor3 = Color3.fromRGB(0,100,180)
Close.Text = "X"
Close.TextColor3 = Color3.new(1,1,1)
Close.TextScaled = true
Close.Font = Enum.Font.GothamBold
Close.Parent = Main

Instance.new("UICorner",Close).CornerRadius = UDim.new(0,15)

local Reopen = Instance.new("TextButton")
Reopen.Size = UDim2.fromOffset(230,58)
Reopen.Position = UDim2.new(0.5,-115,0.90,0)
Reopen.BackgroundColor3 = Color3.fromRGB(0,105,190)
Reopen.Text = "🌊 MỞ LẠI MENU"
Reopen.TextColor3 = Color3.new(1,1,1)
Reopen.TextScaled = true
Reopen.Font = Enum.Font.GothamBold
Reopen.Visible = false
Reopen.Parent = gui

Instance.new("UICorner",Reopen).CornerRadius = UDim.new(0,15)

Close.MouseButton1Click:Connect(function()
	Main.Visible = false
	Reopen.Visible = true
end)

Reopen.MouseButton1Click:Connect(function()
	Main.Visible = true
	Reopen.Visible = false
end)

local Tabs = Instance.new("Frame")
Tabs.Size = UDim2.new(1,-20,0,55)
Tabs.Position = UDim2.fromOffset(10,75)
Tabs.BackgroundTransparency = 1
Tabs.Parent = Main

local tabLayout = Instance.new("UIListLayout")
tabLayout.FillDirection = Enum.FillDirection.Horizontal
tabLayout.Padding = UDim.new(0,6)
tabLayout.Parent = Tabs

local Content = Instance.new("Frame")
Content.Size = UDim2.new(1,-20,1,-145)
Content.Position = UDim2.fromOffset(10,140)
Content.BackgroundTransparency = 1
Content.Parent = Main

local Pages = {}

local function NewPage()
	local p = Instance.new("ScrollingFrame")
	p.Size = UDim2.fromScale(1,1)
	p.BackgroundTransparency = 1
	p.BorderSizePixel = 0
	p.ScrollBarThickness = 4
	p.CanvasSize = UDim2.new()
	p.Visible = false
	p.Parent = Content

	local l = Instance.new("UIListLayout")
	l.Padding = UDim.new(0,8)
	l.Parent = p

	l:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		p.CanvasSize = UDim2.fromOffset(0,l.AbsoluteContentSize.Y+15)
	end)

	table.insert(Pages,p)
	return p
end

local TsunamiPage = NewPage()
local ProtectionPage = NewPage()
local OwnershipPage = NewPage()
local PlayerPage = NewPage()
local SkyPage = NewPage()

local function MakeTab(text,icon,page)
	local b = Instance.new("TextButton")
	b.Size = UDim2.fromOffset(155,55)
	b.BackgroundColor3 = Color3.fromRGB(0,65,120)
	b.Text = icon.." "..text
	b.TextColor3 = Color3.fromRGB(230,250,255)
	b.TextScaled = true
	b.Font = Enum.Font.GothamBold
	b.Parent = Tabs

	Instance.new("UICorner",b).CornerRadius = UDim.new(0,12)

	b.MouseButton1Click:Connect(function()
		for _,x in ipairs(Pages) do
			x.Visible = false
		end

		page.Visible = true

		for _,other in ipairs(Tabs:GetChildren()) do
			if other:IsA("TextButton") then
				other.BackgroundColor3 = Color3.fromRGB(0,65,120)
			end
		end

		b.BackgroundColor3 = Color3.fromRGB(0,130,220)
	end)
end

local function Button(parent,text,callback)
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1,-8,0,54)
	b.BackgroundColor3 = Color3.fromRGB(0,60,110)
	b.Text = text
	b.TextColor3 = Color3.fromRGB(235,250,255)
	b.TextScaled = true
	b.Font = Enum.Font.GothamBold
	b.Parent = parent

	Instance.new("UICorner",b).CornerRadius = UDim.new(0,12)

	local st = Instance.new("UIStroke")
	st.Color = Color3.fromRGB(0,155,235)
	st.Thickness = 1.5
	st.Parent = b

	b.MouseButton1Click:Connect(callback)

	return b
end

local currentWave

local function CreateWave(color)
	if currentWave then
		currentWave:Destroy()
	end

	local char = LP.Character
	if not char then return end

	local root = char:FindFirstChild("HumanoidRootPart")
	if not root then return end

	local wave = Instance.new("Part")
	wave.Name = "TSUNAMI_PREMIER_WAVE"
	wave.Anchored = true
	wave.CanCollide = false
	wave.Material = Enum.Material.Neon
	wave.Color = color
	wave.Transparency = 0.12
	wave.Size = Vector3.new(500,500,80)
	wave.CFrame = root.CFrame * CFrame.new(0,250,-250)
	wave.Parent = workspace

	currentWave = wave

	local direction = root.CFrame.LookVector
	local speed = 50

	task.spawn(function()
		while wave.Parent do
			local dt = RunService.Heartbeat:Wait()
			wave.CFrame += direction * speed * dt
		end
	end)
end

Button(TsunamiPage,"🌊 BLUE TSUNAMI",function()
	CreateWave(Color3.fromRGB(0,170,255))
end)

Button(TsunamiPage,"🔴 RED TSUNAMI",function()
	CreateWave(Color3.fromRGB(255,40,40))
end)

Button(TsunamiPage,"🌊 SPAWN TSUNAMI",function()
	CreateWave(Color3.fromRGB(0,170,255))
end)

Button(TsunamiPage,"🗑️ REMOVE TSUNAMI",function()
	if currentWave then
		currentWave:Destroy()
		currentWave = nil
	end
end)

Button(TsunamiPage,"📏 RADIUS 500 MÉT",function() end)
Button(TsunamiPage,"↔️ CHIỀU RỘNG 500 MÉT",function() end)
Button(TsunamiPage,"↕️ CHIỀU CAO 500 MÉT",function() end)
Button(TsunamiPage,"⚡ TỐC ĐỘ 50 KM/H",function() end)

local protectionNames = {
	"🛡️ PROTECTION",
	"✋ ANTI GRAB",
	"💨 ANTI FLING",
	"🧍 ANTI RAGDOLL",
	"🚫 ANTI KICK",
	"♾️ ANTI LOOP",
	"🎮 ANTI INPUT",
	"⚡ ANTI LAG",
	"🚀 ANTI TOY FLING",
	"🔥 ANTI BURN",
	"💥 ANTI EXPLOSION",
	"❄️ ANTI SNOWBALL",
	"☠️ ANTI POISON"
}

for _,name in ipairs(protectionNames) do
	Button(ProtectionPage,name,function() end)
end

Button(OwnershipPage,"👑 CHỌN NGƯỜI CHƠI",function() end)

for _,p in ipairs(Players:GetPlayers()) do
	if p ~= LP then
		Button(OwnershipPage,"👤 "..p.Name,function() end)
	end
end

Button(OwnershipPage,"❌ KICK PLAYER",function() end)
Button(OwnershipPage,"👥 WHITELIST FRIENDS",function() end)

Button(PlayerPage,"🏃 WALKSPEED 50",function()
	local c = LP.Character
	local h = c and c:FindFirstChildOfClass("Humanoid")
	if h then h.WalkSpeed = 50 end
end)

Button(PlayerPage,"🦘 JUMPPOWER 50",function()
	local c = LP.Character
	local h = c and c:FindFirstChildOfClass("Humanoid")
	if h then
		h.UseJumpPower = true
		h.JumpPower = 50
	end
end)

Button(PlayerPage,"🔄 RESET CHARACTER",function()
	local c = LP.Character
	local h = c and c:FindFirstChildOfClass("Humanoid")
	if h then h.Health = 0 end
end)

Button(PlayerPage,"♾️ INFINITE JUMP",function() end)
Button(PlayerPage,"🛡️ NO FALL DAMAGE",function() end)
Button(PlayerPage,"👻 NOCLIP",function() end)
Button(PlayerPage,"👁️ ESP PLAYERS",function() end)

Button(SkyPage,"🔵 BẦU TRỜI XANH",function()
	Lighting.Ambient = Color3.fromRGB(80,170,255)
	Lighting.OutdoorAmbient = Color3.fromRGB(80,170,255)
end)

Button(SkyPage,"🔴 BẦU TRỜI ĐỎ",function()
	Lighting.Ambient = Color3.fromRGB(255,60,60)
	Lighting.OutdoorAmbient = Color3.fromRGB(255,60,60)
end)

Button(SkyPage,"🟣 BẦU TRỜI TÍM TỐI",function()
	Lighting.Ambient = Color3.fromRGB(70,20,110)
	Lighting.OutdoorAmbient = Color3.fromRGB(70,20,110)
end)

Button(SkyPage,"🟩 MINECRAFT",function()
	Lighting.Ambient = Color3.fromRGB(100,180,100)
	Lighting.OutdoorAmbient = Color3.fromRGB(100,180,100)
end)

Button(SkyPage,"🟠 HOÀNG HÔN",function()
	Lighting.Ambient = Color3.fromRGB(255,140,50)
	Lighting.OutdoorAmbient = Color3.fromRGB(255,140,50)
end)

MakeTab("SÓNG","🌊",TsunamiPage)
MakeTab("BẢO VỆ","🛡️",ProtectionPage)
MakeTab("OWNER","👑",OwnershipPage)
MakeTab("PLAYER","👤",PlayerPage)
MakeTab("TRỜI","🌌",SkyPage)

TsunamiPage.Visible = true

local dragging = false
local dragStart
local startPosition

Main.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPosition = Main.Position
	end
end)

UIS.InputChanged:Connect(function(input)
	if not dragging then return end

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseMovement then

		local delta = input.Position - dragStart

		Main.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)
	end
end)

UIS.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
	end
end)
